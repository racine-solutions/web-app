import { SmsEventConfigurationResolver } from './sms.event.configuration.resolver';

describe('SmsEventConfigurationResolver', () => {
  it('should create an instance', () => {
    expect(new SmsEventConfigurationResolver()).toBeTruthy();
  });
});
